# PersistentVolumeClaim for Appointment Database
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: appointment-db-pvc
  namespace: healthcare
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
---
# Deployment for Appointment Database
apiVersion: apps/v1
kind: Deployment
metadata:
  name: appointment-db
  namespace: healthcare
  labels:
    app: appointment-db
    service: mongodb
spec:
  replicas: 1
  selector:
    matchLabels:
      app: appointment-db
  template:
    metadata:
      labels:
        app: appointment-db
        service: mongodb
    spec:
      containers:
      - name: mongodb
        image: mongo:latest
        ports:
        - containerPort: 27017
          name: mongodb
        env:
        - name: MONGO_INITDB_DATABASE
          value: "appointmentdb"
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "250m"
        volumeMounts:
        - name: appointment-db-storage
          mountPath: /data/db
        livenessProbe:
          exec:
            command:
            - mongosh
            - --eval
            - "db.adminCommand('ping')"
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          exec:
            command:
            - mongosh
            - --eval
            - "db.adminCommand('ping')"
          initialDelaySeconds: 5
          periodSeconds: 5
      volumes:
      - name: appointment-db-storage
        persistentVolumeClaim:
          claimName: appointment-db-pvc
---
# Service for Appointment Database
apiVersion: v1
kind: Service
metadata:
  name: appointment-db-service
  namespace: healthcare
  labels:
    app: appointment-db
spec:
  selector:
    app: appointment-db
  ports:
    - protocol: TCP
      port: 27017
      targetPort: 27017
      name: mongodb
  clusterIP: None