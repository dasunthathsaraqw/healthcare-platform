# PersistentVolumeClaim for Payment Database
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: payment-db-pvc
  namespace: healthcare
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
# Deployment for Payment Database
apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-db
  namespace: healthcare
  labels:
    app: payment-db
    service: mongodb
spec:
  replicas: 1
  selector:
    matchLabels:
      app: payment-db
  template:
    metadata:
      labels:
        app: payment-db
        service: mongodb
    spec:
      containers:
      - name: mongodb
        image: mongo:latest
        ports:
        - containerPort: 27017
          name: mongodb
        env:
        - name: MONGO_INITDB_DATABASE
          value: "paymentdb"
        resources:
          requests:
            memory: "128Mi"
            cpu: "50m"
          limits:
            memory: "256Mi"
            cpu: "100m"
        volumeMounts:
        - name: payment-db-storage
          mountPath: /data/db
        livenessProbe:
          exec:
            command:
            - mongosh
            - --eval
            - "db.adminCommand('ping')"
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          exec:
            command:
            - mongosh
            - --eval
            - "db.adminCommand('ping')"
          initialDelaySeconds: 5
          periodSeconds: 5
      volumes:
      - name: payment-db-storage
        persistentVolumeClaim:
          claimName: payment-db-pvc
---
# Service for Payment Database
apiVersion: v1
kind: Service
metadata:
  name: payment-db-service
  namespace: healthcare
  labels:
    app: payment-db
spec:
  selector:
    app: payment-db
  ports:
    - protocol: TCP
      port: 27017
      targetPort: 27017
      name: mongodb
  clusterIP: None